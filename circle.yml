machine:
  python:
    version: 3.5.2  # Use 3.5.2 as the base.

dependencies:
  override:
    - pip install tox tox-pyenv codecov
    - pyenv install 3.6-dev
    - pyenv install 3.7-dev
    - pyenv local 3.3.6 3.4.3 3.5.2 3.6-dev 3.7-dev

test:
  post:
    - codecov
